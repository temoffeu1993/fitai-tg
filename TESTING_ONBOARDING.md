# 🧪 ИНСТРУКЦИЯ ДЛЯ ПРОВЕРКИ ОНБОРДИНГА

## ⚠️ ВАЖНО: Очисти кэш перед тестом!

1. **Открой приложение**
2. **Нажми F12** (открой консоль разработчика)
3. **Нажми правую кнопку мыши** на кнопке обновления в браузере
4. **Выбери "Очистить кэш и жёсткая перезагрузка"** (Hard Reload)

---

## 📋 ТЕСТ 1: Проверка разблокировки кнопок

### Шаги:
1. Сбрось профиль (Профиль → кнопка сброса)
2. Пройди онбординг **ДО КОНЦА**
3. **СМОТРИ В КОНСОЛЬ** (F12) - должны быть логи:

```
✅ Onboarding complete! Flag saved: 1
LayoutWithNav: onb_complete event
NavBar: onboarding check { isComplete: true, flag: '1' }
```

4. После редиректа на Dashboard **проверь в консоли**:

```javascript
localStorage.getItem("onb_complete")
// Должно быть: "1"
```

5. **Проверь кнопки навигации:**
   - Главная ✅
   - Тренировки ✅
   - Питание ✅
   - Профиль ✅

### Если кнопки всё ещё заблокированы:

Выполни в консоли:
```javascript
localStorage.getItem("onb_complete")
// Что показывает? null или "1"?

// Если null - выполни:
localStorage.setItem("onb_complete", "1");
location.reload();
// Кнопки должны разблокироваться
```

---

## 📋 ТЕСТ 2: Проверка моргания чекбокса

### Шаги:
1. Открой экран выбора схемы
2. Нажми на блок "Я ознакомился и согласен..."
3. **Проверь:** блок НЕ должен моргать серым

### Если всё ещё моргает:

Это может быть **кэш браузера**. Попробуй:
1. Ctrl+Shift+R (жёсткая перезагрузка)
2. Или открой в режиме инкогнито
3. Или очисти весь кэш браузера

---

## 🔧 ДОПОЛНИТЕЛЬНАЯ ДИАГНОСТИКА

Выполни в консоли и **пришли мне результаты**:

```javascript
// 1. Проверка флага онбординга
console.log("onb_complete:", localStorage.getItem("onb_complete"));

// 2. Проверка имени из Telegram
console.log("profile:", localStorage.getItem("profile"));

// 3. Проверка всех ключей
console.log("All keys:", Object.keys(localStorage));
```

---

## 🚨 ЕСЛИ НИЧЕГО НЕ ПОМОГАЕТ:

1. **Очисти localStorage полностью:**
```javascript
localStorage.clear();
location.reload();
```

2. **Пройди онбординг заново**

3. **Пришли мне скриншот консоли** после прохождения

---

## ✅ ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:

После прохождения онбординга:
- ✅ Редирект на Dashboard
- ✅ Имя: "Привет, [твоё имя из Telegram]"
- ✅ Все кнопки активны (не серые)
- ✅ Чекбокс не моргает серым

---

## 📞 ЧТО ПРИСЛАТЬ МНЕ:

1. Скриншот консоли после прохождения онбординга
2. Результат: `localStorage.getItem("onb_complete")`
3. Видео/скриншот моргания (если всё ещё моргает)
