# Тестирование новой системы генерации

## 🚀 Быстрый старт

### 1. Запуск API

```bash
cd api
npm run dev
```

API запустится на `http://localhost:3000`

### 2. Получить токен авторизации

Используй существующего пользователя или создай нового через Telegram бот.

Токен можно взять из:
- DevTools → Application → Local Storage → `tg_auth_token`
- Или из ответа `/auth/telegram`

### 3. Тестовые запросы

#### 3.1. Список схем с templates

```bash
curl -X GET http://localhost:3000/api/workout-test/schemes \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

**Ожидаемый результат:**
```json
{
  "total": 2,
  "schemes": [
    {
      "id": "push_pull_legs_5x_classic",
      "name": "Жим–тяга–ноги 5 дней",
      "daysWithTemplates": [
        { "index": 0, "label": "Push A" },
        { "index": 1, "label": "Pull A" },
        { "index": 2, "label": "Legs A" }
      ]
    },
    {
      "id": "full_body_3x_classic",
      "name": "Классика: всё тело 3 раза",
      "daysWithTemplates": [
        { "index": 0, "label": "Full Body A" }
      ]
    }
  ]
}
```

#### 3.2. Генерация тренировки (идеальные условия)

```bash
curl -X POST http://localhost:3000/api/workout-test/generate \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -H "Content-Type: application/json" \
  -d '{
    "schemeId": "push_pull_legs_5x_classic",
    "dayIndex": 0,
    "experience": "intermediate"
  }'
```

**Ожидаемый результат:**
- mode: "normal"
- 5 упражнений
- 16 подходов
- ~60 минут

#### 3.3. Генерация с плохим сном

```bash
curl -X POST http://localhost:3000/api/workout-test/generate \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -H "Content-Type: application/json" \
  -d '{
    "schemeId": "push_pull_legs_5x_classic",
    "dayIndex": 0,
    "experience": "intermediate",
    "checkIn": {
      "sleepHours": 5,
      "energyLevel": "low",
      "stressLevel": "high"
    }
  }'
```

**Ожидаемый результат:**
- mode: "light" или "recovery"
- 3-4 упражнения (вместо 5)
- 10-12 подходов (вместо 16)
- Рекомендация об облегчении

#### 3.4. Генерация с травмой плеча

```bash
curl -X POST http://localhost:3000/api/workout-test/generate \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -H "Content-Type: application/json" \
  -d '{
    "schemeId": "push_pull_legs_5x_classic",
    "dayIndex": 0,
    "experience": "intermediate",
    "checkIn": {
      "sleepHours": 7,
      "energyLevel": "medium",
      "stressLevel": "low",
      "pain": [
        {
          "location": "правое плечо",
          "level": 6
        }
      ]
    }
  }'
```

**Ожидаемый результат:**
- Убраны: жимы над головой, махи на плечи
- Warnings: "Боль в правом плечо - избегаем жимов над головой"
- Упражнения адаптированы (больше жимов лёжа, меньше плеч)

#### 3.5. Критическое состояние

```bash
curl -X POST http://localhost:3000/api/workout-test/generate \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -H "Content-Type: application/json" \
  -d '{
    "schemeId": "push_pull_legs_5x_classic",
    "dayIndex": 0,
    "experience": "intermediate",
    "checkIn": {
      "sleepHours": 3,
      "energyLevel": "low",
      "stressLevel": "very_high",
      "pain": [
        {
          "location": "плечо",
          "level": 8
        }
      ]
    }
  }'
```

**Ожидаемый результат:**
- mode: "skip"
- exercises: []
- Рекомендация: "Рекомендуем полный отдых"

#### 3.6. Только анализ чекина

```bash
curl -X POST http://localhost:3000/api/workout-test/check-in-analysis \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -H "Content-Type: application/json" \
  -d '{
    "schemeId": "push_pull_legs_5x_classic",
    "dayIndex": 0,
    "checkIn": {
      "sleepHours": 6,
      "energyLevel": "medium",
      "stressLevel": "high"
    }
  }'
```

**Ожидаемый результат:**
```json
{
  "analysis": {
    "mode": "light",
    "volumeMultiplier": 0.7,
    "intensityAdjustment": "reduce by 15-20%",
    "maxExercises": 3,
    "recommendation": "..."
  },
  "original": {
    "exercises": 5,
    "sets": 16,
    "duration": 60
  },
  "adapted": {
    "exercises": 3,
    "sets": 10,
    "duration": 45
  }
}
```

## 📊 Что проверять

### 1. Адаптация работает?
- [ ] При плохом сне уменьшается объём
- [ ] При травмах исключаются проблемные упражнения
- [ ] При критическом состоянии предлагается пропуск

### 2. AI подбирает упражнения?
- [ ] Упражнения из базы движений
- [ ] Не повторяются недавние упражнения
- [ ] Есть технические подсказки (cues)
- [ ] Подбираются веса (если есть история)

### 3. Структура сохраняется?
- [ ] Количество упражнений соответствует template
- [ ] Подходы и повторения из template
- [ ] Отдых из template
- [ ] Warmup и cooldown на месте

### 4. Разные схемы?
- [ ] PPL Push A работает
- [ ] PPL Pull A работает
- [ ] PPL Legs A работает
- [ ] Full Body A работает

## 🐛 Известные ограничения

1. **Пока только 4 template**
   - Push A, Pull A, Legs A, Full Body A
   - Остальные дни пока без template

2. **История упражнений захардкожена**
   - В тестовом эндпоинте используется фейковая история
   - Для реальной интеграции нужно подключить БД

3. **Веса подбираются AI**
   - Пока нет реальной истории весов
   - AI делает предположения

4. **Нет сохранения результатов**
   - Тестовый эндпоинт только генерирует
   - Не сохраняет в БД

## 🔜 Следующие шаги

После успешного тестирования:

1. **Добавить больше templates**
   - Push B, Pull B
   - Full Body B, C
   - Upper/Lower схемы

2. **Интегрировать в основной поток**
   - Заменить логику в `/plan/generate`
   - Использовать реальную историю из БД
   - Сохранять результаты

3. **UI для чекина**
   - Форма в webapp
   - Визуализация адаптации
   - Предупреждения

4. **Прогрессия**
   - Автоматический подбор весов
   - Отслеживание прогресса
   - Периодизация

## 💬 Обратная связь

Если что-то не работает:
1. Проверь логи в консоли API
2. Проверь что токен валидный
3. Проверь что schemeId и dayIndex корректные

Логи будут показывать:
- `🧪 ТЕСТИРОВАНИЕ НОВОЙ ЛОГИКИ ГЕНЕРАЦИИ`
- `🎯 ГЕНЕРАЦИЯ ТРЕНИРОВКИ (НОВЫЙ ПОДХОД)`
- `📋 Анализ чекина...`
- `🤖 AI подбор упражнений...`

