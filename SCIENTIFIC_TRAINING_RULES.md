# Научно обоснованные правила генерации тренировок

## 📚 Источники

Данная система основана на:
1. **Принципы силового тренинга** (Wikipedia: Принципы силового тренинга)
2. **Прогрессивная перегрузка** (Wikipedia: Прогрессирующая перегрузка)
3. **Периодизация Матвеева** (Классическая советская школа)
4. **Современные исследования по гипертрофии** (Meta-анализы)
5. **Рекомендации ACSM** (American College of Sports Medicine)
6. **Метод 8×8 Винса Жиронды** (для продвинутых)

---

## 🎯 1. ОБЪЕМ ТРЕНИРОВОК (количество подходов)

### По уровню подготовки:

#### **НОВИЧОК (beginner)** - опыт < 6 месяцев
```typescript
{
  totalSetsPerSession: [8, 12],       // 8-12 подходов за тренировку
  totalSetsPerMusclePerWeek: [6, 10], // 6-10 подходов на группу в неделю
  exercisesPerSession: [3, 5],        // 3-5 упражнений
  focusExercises: "базовые",          // Приоритет базовым
  volumeModifier: 0.7,                // 70% от стандарта
  
  reasoning: "Нервная система адаптируется к нагрузкам, 
             нужно время на освоение техники, 
             высокий риск перетренированности"
}
```

#### **СРЕДНИЙ (intermediate)** - опыт 6 месяцев - 2 года
```typescript
{
  totalSetsPerSession: [12, 18],      // 12-18 подходов
  totalSetsPerMusclePerWeek: [10, 15], // 10-15 подходов на группу в неделю
  exercisesPerSession: [4, 6],        // 4-6 упражнений
  focusExercises: "базовые + изоляция",
  volumeModifier: 1.0,                // 100% стандарт
  
  reasoning: "Нервная система адаптирована, 
             можно повышать объем для роста,
             баланс базовых и изоляции"
}
```

#### **ПРОДВИНУТЫЙ (advanced)** - опыт > 2 лет
```typescript
{
  totalSetsPerSession: [15, 22],      // 15-22 подхода
  totalSetsPerMusclePerWeek: [15, 20], // 15-20 подходов на группу в неделю
  exercisesPerSession: [5, 7],        // 5-7 упражнений
  focusExercises: "базовые + изоляция + спец. техники",
  volumeModifier: 1.3,                // 130% от стандарта
  advancedTechniques: ["supersets", "dropsets", "rest-pause"],
  
  reasoning: "Высокая восстановительная способность,
             нужен больший объем для стимула роста,
             можно использовать интенсивные техники"
}
```

---

## 🎯 2. ИНТЕНСИВНОСТЬ И ПОВТОРЕНИЯ

### По целям:

#### **СИЛА (strength)**
```typescript
{
  repsRange: "3-6",
  intensity: "85-95% от 1ПМ",  // 1ПМ = одноповторный максимум
  setsPerExercise: [4, 6],
  tempo: "explosive",           // Взрывной темп
  restBetweenSets: [180, 240],  // 3-4 минуты (длинный отдых)
  focus: "Нейромышечная адаптация",
  
  exerciseRatio: {
    main_lift: "70%",           // 70% - главное базовое
    secondary: "20%",           // 20% - вторичное
    accessory: "10%"            // 10% - вспомогательные
  },
  
  reasoning: "Короткие подходы с высокой интенсивностью,
             длинный отдых для восстановления ЦНС,
             акцент на нейромышечной эффективности"
}
```

#### **ГИПЕРТРОФИЯ (build_muscle)**
```typescript
{
  repsRange: "6-12",
  intensity: "65-85% от 1ПМ",
  setsPerExercise: [3, 5],
  tempo: "controlled (3-1-1-0)",  // 3 сек негативная, 1 пауза, 1 позитивная, 0 пауза
  restBetweenSets: [60, 120],     // 1-2 минуты (умеренный)
  focus: "Механическое напряжение + метаболический стресс",
  
  exerciseRatio: {
    main_lift: "40%",       // 40% - главное базовое
    secondary: "30%",       // 30% - вторичное базовое
    accessory: "20%",       // 20% - изоляция крупных мышц
    isolation: "10%"        // 10% - мелкая изоляция
  },
  
  TUT: [40, 70],  // Time Under Tension: 40-70 секунд за подход
  
  reasoning: "Средние повторения создают оптимальный баланс 
             механического напряжения и метаболического стресса,
             умеренный отдых поддерживает метаболический стресс"
}
```

#### **ВЫНОСЛИВОСТЬ / СБРОС ВЕСА (lose_weight)**
```typescript
{
  repsRange: "12-20",
  intensity: "50-65% от 1ПМ",
  setsPerExercise: [2, 4],
  tempo: "fast",
  restBetweenSets: [30, 60],      // 30-60 секунд (короткий)
  focus: "Метаболический стресс + калорийный расход",
  
  style: "circuit_or_supersets",  // Круговая или суперсеты
  
  exerciseRatio: {
    compound: "60%",        // 60% - многосуставные
    isolation: "20%",       // 20% - изоляция
    cardio_finisher: "20%"  // 20% - кардио/метаболический финишер
  },
  
  EPOC: "high",  // Высокий Excess Post-exercise Oxygen Consumption
  
  reasoning: "Высокие повторения, короткий отдых повышают 
             метаболический отклик, сжигают больше калорий,
             круговые тренировки поддерживают ЧСС"
}
```

#### **ФУНКЦИОНАЛЬНОСТЬ / АТЛЕТИЗМ (athletic_body)**
```typescript
{
  repsRange: "8-12",
  intensity: "70-80% от 1ПМ",
  setsPerExercise: [3, 4],
  tempo: "mixed",  // Смешанный: explosive на позитивной, controlled на негативной
  restBetweenSets: [60, 90],
  focus: "Баланс силы, скорости, выносливости",
  
  exerciseRatio: {
    compound: "50%",
    functional: "30%",  // Функциональные движения
    core: "10%",
    plyometric: "10%"   // Плиометрика
  },
  
  reasoning: "Баланс между силой и метаболизмом,
             функциональные движения для реальной жизни"
}
```

---

## 🎯 3. АДАПТАЦИЯ ПОД ВРЕМЯ

### Формула расчета:

```
Время на упражнение = (sets × reps × 3 сек) + (sets × rest)

Пример:
Упражнение: 4 подхода × 8 повторений, отдых 120 сек
Время = (4 × 8 × 3) + (4 × 120) = 96 + 480 = 576 сек = ~10 минут
```

### Структура тренировки:

```typescript
type SessionTimeBreakdown = {
  warmup: number,      // 5-10 минут (зависит от общего времени)
  working: number,     // 60-80% времени
  cooldown: number     // 5-10 минут
}

// Примеры:
45_MIN_SESSION = {
  warmup: 5,     // 11%
  working: 35,   // 78%
  cooldown: 5    // 11%
}

60_MIN_SESSION = {
  warmup: 7,     // 12%
  working: 46,   // 77%
  cooldown: 7    // 11%
}

90_MIN_SESSION = {
  warmup: 10,    // 11%
  working: 70,   // 78%
  cooldown: 10   // 11%
}
```

### Расчет количества упражнений:

```typescript
function calculateMaxExercises(
  totalMinutes: number,
  goal: Goal,
  experience: Experience
): number {
  
  const warmupCooldown = totalMinutes * 0.22;  // 22% на разминку/заминку
  const workingTime = totalMinutes - warmupCooldown;
  
  // Время на одно упражнение (зависит от цели)
  const timePerExercise = {
    strength: 12,      // 12 минут (тяжелые подходы с длинным отдыхом)
    hypertrophy: 8,    // 8 минут (средние подходы)
    metabolic: 6,      // 6 минут (короткий отдых)
    athletic: 7        // 7 минут
  }[goal];
  
  const maxExercises = Math.floor(workingTime / timePerExercise);
  
  // Корректировка под опыт
  const experienceModifier = {
    beginner: 0.7,
    intermediate: 1.0,
    advanced: 1.2
  }[experience];
  
  return Math.round(maxExercises * experienceModifier);
}

// Примеры:
// 60 мин, гипертрофия, intermediate:
// working = 60 * 0.78 = 47 мин
// exercises = 47 / 8 = 5.8 → 6 упражнений

// 60 мин, гипертрофия, beginner:
// exercises = 6 * 0.7 = 4.2 → 4 упражнения

// 90 мин, гипертрофия, advanced:
// working = 90 * 0.78 = 70 мин
// exercises = 70 / 8 = 8.7 → 9 упражнений
// 9 * 1.2 = 10.8 → но ограничим до 8 (чтобы не убить)
```

---

## 🎯 4. РАСПРЕДЕЛЕНИЕ ПРИОРИТЕТОВ

### Иерархия важности упражнений:

```typescript
type ExercisePriority = {
  priority: 1 | 2 | 3 | 4 | 5,
  role: "main_lift" | "secondary" | "accessory" | "isolation" | "optional",
  mandatory: boolean,
  canSkipIf: string[]
}

// Пример для Push Day (Гипертрофия):
[
  {
    priority: 1,
    role: "main_lift",           // Главное базовое
    name: "Горизонтальный жим",
    mandatory: true,
    canSkipIf: [],
    volumePercent: 30,           // 30% от общего объема (подходов)
    
    reasoning: "Фундамент тренировки, максимальная отдача"
  },
  {
    priority: 2,
    role: "secondary",           // Вторичное базовое
    name: "Наклонный жим",
    mandatory: true,
    canSkipIf: ["time < 50"],   // Можно пропустить если < 50 минут
    volumePercent: 25,
    
    reasoning: "Проработка под другим углом"
  },
  {
    priority: 3,
    role: "accessory",           // Вспомогательное
    name: "Жим на плечи",
    mandatory: false,
    canSkipIf: ["time < 60", "experience: beginner"],
    volumePercent: 20,
    
    reasoning: "Дополнительный объем на плечи"
  },
  {
    priority: 4,
    role: "isolation",
    name: "Изоляция плеч",
    mandatory: false,
    canSkipIf: ["time < 70", "experience: beginner"],
    volumePercent: 15,
    
    reasoning: "Детализация, не критична"
  },
  {
    priority: 5,
    role: "isolation",
    name: "Изоляция трицепса",
    mandatory: false,
    canSkipIf: ["time < 60", "goal: strength"],
    volumePercent: 10,
    
    reasoning: "Доработка малых мышц"
  }
]
```

---

## 🎯 5. ВРЕМЯ ОТДЫХА (научные данные)

### Зависимость от цели:

| Цель | Отдых | Почему |
|------|-------|--------|
| **Сила** | 3-5 минут | Восстановление АТФ-КФ системы и ЦНС |
| **Гипертрофия** | 1-2 минуты | Баланс механического напряжения и метаболического стресса |
| **Выносливость** | 30-60 секунд | Поддержание метаболического стресса |
| **Функциональность** | 60-90 секунд | Баланс |

### Зависимость от типа упражнения:

```typescript
const restByExerciseType = {
  main_lift: {
    strength: 240,      // 4 минуты
    hypertrophy: 120,   // 2 минуты
    metabolic: 90       // 1.5 минуты
  },
  secondary: {
    strength: 180,      // 3 минуты
    hypertrophy: 90,    // 1.5 минуты
    metabolic: 60       // 1 минута
  },
  accessory: {
    strength: 120,      // 2 минуты
    hypertrophy: 75,    // 1.25 минуты
    metabolic: 45       // 45 секунд
  },
  isolation: {
    strength: null,     // Изоляция не нужна для силы
    hypertrophy: 60,    // 1 минута
    metabolic: 30       // 30 секунд
  }
};
```

### Корректировка под опыт:

```typescript
const restModifierByExperience = {
  beginner: 1.2,    // +20% отдыха (нужно больше времени)
  intermediate: 1.0, // Стандарт
  advanced: 0.9     // -10% отдыха (быстрее восстанавливаются)
};
```

---

## 🎯 6. ПРОГРЕССИВНАЯ ПЕРЕГРУЗКА

### Принципы увеличения нагрузки:

```typescript
const progressionRules = {
  beginner: {
    increaseFrequency: "every 2 weeks",
    weightIncrease: "2.5-5%",
    volumeIncrease: "1 set or 2 reps",
    
    reasoning: "Медленная адаптация, риск травм при быстром росте"
  },
  
  intermediate: {
    increaseFrequency: "every 1-2 weeks",
    weightIncrease: "2.5-5%",
    volumeIncrease: "1-2 sets or 2-3 reps",
    
    reasoning: "Быстрая адаптация, но нужен баланс"
  },
  
  advanced: {
    increaseFrequency: "weekly",
    weightIncrease: "2.5-5% or use periodization",
    volumeIncrease: "variable (depends on phase)",
    techniques: ["DUP", "wave loading", "block periodization"],
    
    reasoning: "Требуется периодизация для преодоления плато"
  }
};
```

### Правило прогрессии (2-2-2):

```
Если выполнил ВСЕ подходы с ВЕРХНЕЙ границей повторений 
на 2 тренировках подряд → увеличивай вес на 2.5-5%

Пример:
Неделя 1: Жим лёжа 60 кг × 4 подхода × 8 повторений (цель 6-8)
Неделя 2: Жим лёжа 60 кг × 4 подхода × 8 повторений ✅
→ Неделя 3: Жим лёжа 62.5 кг × 4 подхода × 6-8 повторений
```

---

## 🎯 7. ИНТЕНСИВНЫЕ ТЕХНИКИ (для продвинутых)

### Когда использовать:

```typescript
const advancedTechniques = {
  supersets: {
    when: "experience: advanced AND goal: hypertrophy",
    benefit: "Увеличение метаболического стресса без доп. времени",
    example: "Жим лёжа → Тяга штанги (антагонисты)",
    rest: "60-90 сек после суперсета"
  },
  
  dropsets: {
    when: "experience: advanced AND goal: hypertrophy AND time: limited",
    benefit: "Полный отказ мышц, экономия времени",
    example: "Махи гантелями: 12 кг × 10 → 8 кг × 10 → 5 кг × max",
    frequency: "не чаще 1-2 раз за тренировку"
  },
  
  rest_pause: {
    when: "experience: advanced AND goal: strength OR hypertrophy",
    benefit: "Дополнительный объем без длинного отдыха",
    example: "6 повторений → отдых 15 сек → 2-3 повторения → 15 сек → 1-2",
    caution: "Очень утомительно, не более 1 упражнения за тренировку"
  },
  
  method_8x8: {
    when: "experience: advanced AND goal: hypertrophy + fat_loss",
    description: "8 подходов × 8 повторений, отдых 30-45 сек",
    inventor: "Винс Жиронда",
    benefit: "Экстремальный метаболический стресс",
    caution: "Очень интенсивно, использовать периодически"
  }
};
```

---

## 🎯 8. ИТОГОВАЯ ФОРМУЛА ГЕНЕРАЦИИ

```typescript
function generateWorkoutRules(params: {
  experience: Experience,
  goal: Goal,
  timeAvailable: number,
  daysPerWeek: number
}) {
  
  // 1. Базовый объем
  const baseVolume = getBaseVolumeByGoal(goal);
  
  // 2. Корректировка под опыт
  const adjustedVolume = baseVolume * experienceModifier[experience];
  
  // 3. Расчет количества упражнений под время
  const maxExercises = calculateMaxExercises(timeAvailable, goal, experience);
  
  // 4. Распределение объема по приоритетам
  const exerciseBlocks = distributeVolumeByPriority(
    adjustedVolume,
    maxExercises,
    goal
  );
  
  // 5. Определение параметров
  const repsRange = repsByGoal[goal];
  const restTimes = restByGoal[goal];
  const tempo = tempoByGoal[goal];
  
  // 6. Проверка на перетренированность
  if (adjustedVolume > getMaxRecoverableVolume(experience, daysPerWeek)) {
    adjustedVolume = getMaxRecoverableVolume(experience, daysPerWeek);
    console.warn("Объем снижен до восстанавливаемого максимума");
  }
  
  return {
    totalSets: adjustedVolume,
    exercises: exerciseBlocks,
    repsRange,
    restTimes,
    tempo,
    estimatedDuration: calculateDuration(exerciseBlocks, restTimes)
  };
}
```

---

## ✅ ВАЛИДАЦИЯ ПРАВИЛ

### Проверка на переработку:

```typescript
const MAX_RECOVERABLE_VOLUME = {
  beginner: {
    perSession: 12,
    perMusclePerWeek: 10
  },
  intermediate: {
    perSession: 18,
    perMusclePerWeek: 15
  },
  advanced: {
    perSession: 22,
    perMusclePerWeek: 20
  }
};

// Если превышаем → предупреждение или автоснижение
```

---

Эта система основана на **научных данных** и **профессиональных методиках**. 

Готов внедрять? 🚀

