# Новая система генерации тренировок

## 🎯 Концепция

**Двухэтапный подход:**
1. **Схема = ПРАВИЛА** - профессионально составленные шаблоны с четкой структурой
2. **AI = УПРАЖНЕНИЯ** - умный подбор конкретных упражнений под правила и состояние пользователя

## 📁 Структура файлов

### Новые файлы:

1. **`workoutTemplates.ts`** - типы и база движений
   - Типы движений (`MovementPattern`)
   - Структура блока упражнений (`ExerciseBlock`)
   - Шаблон дня (`DayTemplate`)
   - База упражнений по типам (`MOVEMENT_PATTERNS_DB`)

2. **`schemeTemplates.ts`** - готовые шаблоны дней
   - `PPL_PUSH_A_TEMPLATE` - Push день из PPL
   - `PPL_PULL_A_TEMPLATE` - Pull день из PPL
   - `PPL_LEGS_A_TEMPLATE` - Legs день из PPL
   - `FULL_BODY_A_TEMPLATE` - Full Body день

3. **`checkInAdapter.ts`** - анализ чекина и адаптация
   - `analyzeCheckIn()` - анализирует состояние пользователя
   - `adaptDayTemplate()` - адаптирует шаблон под чекин

4. **`workoutGenerator.ts`** - генерация тренировок
   - `generateWorkoutFromTemplate()` - главная функция генерации

5. **`workoutTest.ts`** - тестовые эндпоинты
   - `POST /api/workout-test/generate` - генерация тренировки
   - `GET /api/workout-test/schemes` - список схем с template
   - `POST /api/workout-test/check-in-analysis` - анализ чекина

### Обновленные файлы:

1. **`workoutSchemes.ts`**
   - Добавлено поле `template?: DayTemplate` в `dayLabels`
   - Импортированы готовые templates

2. **`index.ts`**
   - Подключен роутер `/api/workout-test`

## 🔄 Как это работает

### 1. Пользователь выбирает схему

На основе онбординга (цель, опыт, дни, время) система предлагает подходящие схемы.

```typescript
// Схема содержит:
{
  id: "push_pull_legs_5x_classic",
  russianName: "Жим–тяга–ноги 5 дней",
  daysPerWeek: 5,
  dayLabels: [
    {
      day: 1,
      label: "Push A",
      focus: "Грудь, передняя часть плеч и трицепс",
      template: PPL_PUSH_A_TEMPLATE // 🔥 Полная структура дня
    }
  ]
}
```

### 2. Перед тренировкой - чекин

Пользователь отвечает на вопросы:
- Сколько спал?
- Уровень энергии?
- Есть боли/травмы?
- Уровень стресса?

```typescript
const checkIn: CheckInData = {
  sleepHours: 6,
  energyLevel: "medium",
  stressLevel: "high",
  pain: [
    { location: "правое плечо", level: 6 }
  ]
};
```

### 3. Анализ чекина

Система анализирует состояние и выдает рекомендации:

```typescript
const analysis = analyzeCheckIn(checkIn, dayTemplate);

// Результат:
{
  mode: "light",                    // Облегченный режим
  volumeMultiplier: 0.7,            // -30% объема
  intensityAdjustment: "reduce by 15-20%",
  maxExercises: 3,                  // Вместо 5
  excludedZones: ["плечи"],
  avoidExercises: ["overhead_press", "lateral_raise"],
  recommendation: "Облегчённая тренировка: 3 упражнения..."
}
```

### 4. Адаптация шаблона

Template адаптируется под результаты чекина:

```typescript
const adaptedTemplate = adaptDayTemplate(dayTemplate, analysis);

// Было: 5 упражнений, 16 подходов
// Стало: 3 упражнения, 10 подходов
// Убраны: упражнения на плечи
```

### 5. AI подбирает упражнения

AI получает:
- Адаптированный template (структура)
- База движений (откуда выбирать)
- История (что не повторять)
- Профиль пользователя (опыт, травмы)

```typescript
const workout = await generateWorkoutFromTemplate({
  dayTemplate: adaptedTemplate,
  dayLabel: "Push A",
  dayFocus: "Грудь и трицепс",
  checkIn,
  history: {
    recentExercises: ["Жим лёжа", "Подтягивания"],
    weightHistory: { "Жим лёжа": "60 кг" }
  },
  userProfile: {
    experience: "intermediate",
    injuries: []
  }
});
```

AI возвращает:

```json
{
  "exercises": [
    {
      "name": "Жим гантелей горизонтально",
      "sets": 4,
      "reps": "6-8",
      "rest": 180,
      "weight": "2×25 кг",
      "cues": "Лопатки сведены, контроль негативной фазы"
    },
    {
      "name": "Жим на наклонной 30°",
      "sets": 3,
      "reps": "8-12",
      "rest": 120,
      "weight": "50 кг",
      "cues": "Полная амплитуда, грудь вверх"
    },
    {
      "name": "Разгибания на блоке",
      "sets": 3,
      "reps": "12-15",
      "rest": 60,
      "weight": "20 кг",
      "cues": "Локти зафиксированы, полное сокращение"
    }
  ]
}
```

## 🧪 Тестирование

### 1. Список схем с templates

```bash
GET /api/workout-test/schemes
```

Ответ:
```json
{
  "total": 2,
  "schemes": [
    {
      "id": "push_pull_legs_5x_classic",
      "name": "Жим–тяга–ноги 5 дней",
      "daysWithTemplates": [
        { "index": 0, "label": "Push A", "focus": "..." },
        { "index": 1, "label": "Pull A", "focus": "..." },
        { "index": 2, "label": "Legs A", "focus": "..." }
      ]
    }
  ]
}
```

### 2. Генерация тренировки

```bash
POST /api/workout-test/generate
Content-Type: application/json

{
  "schemeId": "push_pull_legs_5x_classic",
  "dayIndex": 0,
  "experience": "intermediate",
  "checkIn": {
    "sleepHours": 6,
    "energyLevel": "medium",
    "stressLevel": "high",
    "pain": [
      { "location": "правое плечо", "level": 6 }
    ]
  }
}
```

Ответ:
```json
{
  "success": true,
  "workout": {
    "title": "Push A",
    "mode": "light",
    "exercises": [...],
    "totalExercises": 3,
    "totalSets": 10,
    "estimatedDuration": 45,
    "adaptationNotes": "Облегчённая тренировка...",
    "warnings": ["Боль в правом плече - избегаем жимов над головой"]
  }
}
```

### 3. Только анализ чекина

```bash
POST /api/workout-test/check-in-analysis
Content-Type: application/json

{
  "schemeId": "push_pull_legs_5x_classic",
  "dayIndex": 0,
  "checkIn": {
    "sleepHours": 4,
    "energyLevel": "low",
    "stressLevel": "very_high"
  }
}
```

Ответ:
```json
{
  "analysis": {
    "mode": "recovery",
    "shouldSkip": false,
    "volumeMultiplier": 0.5,
    "recommendation": "Восстановительная тренировка: 2 упражнения..."
  },
  "original": {
    "exercises": 5,
    "sets": 16,
    "duration": 60
  },
  "adapted": {
    "exercises": 2,
    "sets": 8,
    "duration": 35
  }
}
```

## 📊 Правила адаптации

### Режимы тренировки:

1. **SKIP** - пропуск
   - Боль 8+/10
   - Сон < 3 часов + энергия LOW
   - Критическое состояние

2. **RECOVERY** - восстановление
   - Боль 7/10
   - Сон < 4 часов
   - Энергия LOW + стресс HIGH
   - Объем: -50%, интенсивность: -30-40%

3. **LIGHT** - облегченный
   - Боль 5-6/10
   - Сон 4-6 часов
   - Энергия MEDIUM, стресс HIGH
   - Объем: -30%, интенсивность: -15-20%

4. **NORMAL** - обычный
   - Нормальное состояние
   - Без изменений

5. **PUSH** - интенсивный
   - Сон 8+ часов
   - Энергия HIGH
   - Стресс LOW
   - Объем: +10%, можно работать тяжелее

### Исключения по болям:

- **Плечо** → убрать жимы над головой, махи
- **Локоть** → убрать изоляцию рук
- **Колено** → убрать приседания, выпады
- **Спина** → убрать осевую нагрузку (приседания, становая)

## 🚀 Дальнейшие шаги

### Что уже сделано:
- ✅ Типы и структуры
- ✅ База движений (60+ упражнений)
- ✅ Анализ чекина
- ✅ Адаптация template
- ✅ AI подбор упражнений
- ✅ Тестовые эндпоинты
- ✅ 4 готовых template (Push A, Pull A, Legs A, Full Body A)

### Что нужно добавить:

1. **Больше templates**
   - Push B, Pull B для PPL
   - Full Body B, C
   - Upper/Lower схемы
   - Женские схемы

2. **Интеграция в основной поток**
   - Заменить старую логику в `plan.ts`
   - Использовать новую генерацию в `/plan/generate`

3. **История и прогрессия**
   - Сохранение выполненных упражнений
   - Автоматический подбор весов на основе истории
   - Прогрессивная перегрузка

4. **UI для чекина**
   - Форма чекина в webapp
   - Визуализация адаптации
   - Предупреждения и рекомендации

## 💡 Преимущества нового подхода

1. **Для пользователя:**
   - ✅ Понятная структура тренировок
   - ✅ Адаптация под состояние
   - ✅ Безопасность (учет травм)
   - ✅ Разнообразие (упражнения меняются)

2. **Для AI:**
   - ✅ Простая задача (выбрать из списка)
   - ✅ Нет противоречий
   - ✅ 1 вызов вместо 3
   - ✅ Меньше токенов

3. **Для разработки:**
   - ✅ Легко создавать схемы
   - ✅ Легко тестировать
   - ✅ Легко улучшать
   - ✅ Модульная архитектура

## 📝 Пример полного потока

```typescript
// 1. Пользователь выбрал схему "PPL 5 дней"
const scheme = workoutSchemes.find(s => s.id === "push_pull_legs_5x_classic");

// 2. Сегодня день 0 (Push A)
const day = scheme.dayLabels[0];
const template = day.template; // PPL_PUSH_A_TEMPLATE

// 3. Чекин
const checkIn = {
  sleepHours: 6,
  energyLevel: "medium",
  stressLevel: "high",
  pain: [{ location: "правое плечо", level: 6 }]
};

// 4. Генерация
const workout = await generateWorkoutFromTemplate({
  dayTemplate: template,
  dayLabel: day.label,
  dayFocus: day.focus,
  checkIn,
  history: getUserHistory(),
  userProfile: getUserProfile()
});

// 5. Результат
console.log(workout);
// {
//   mode: "light",
//   exercises: [
//     { name: "Жим гантелей горизонтально", sets: 3, reps: "6-8", ... },
//     { name: "Жим на наклонной 30°", sets: 3, reps: "8-12", ... },
//     { name: "Разгибания на блоке", sets: 3, reps: "12-15", ... }
//   ],
//   adaptationNotes: "Облегчённая тренировка, избегаем плечи",
//   warnings: ["Боль в правом плече - избегаем жимов над головой"]
// }
```

---

**Готово к тестированию!** 🎉

Запустите API и попробуйте эндпоинты `/api/workout-test/*`

