# 🚀 Quick Start: Новая система схем

## TL;DR

```bash
cd api
psql -U username -d database -f sql/2025_12_workout_schemes.sql
psql -U username -d database -f sql/seed_workout_schemes_v2.sql

# Запустить приложение
npm run dev  # API
cd ../webapp && npm run dev  # Webapp
```

Готово! Теперь у вас:
- ✅ 26 профессиональных схем
- ✅ Выбор времени тренировки в онбординге
- ✅ Интеллектуальный подбор с ИИ
- ✅ Персональные обоснования

---

## Что изменилось за 60 секунд

### 1️⃣ Выбор времени в онбординге
Пользователь теперь выбирает длительность: **30, 45, 60, 75, 90 минут**

### 2️⃣ 26 схем вместо 9
- Адаптированный PPL под 3 дня
- Схемы для женщин с акцентом на ягодицы
- Жиросжигание, здоровье, сила
- Экспресс-схемы 30-45 минут

### 3️⃣ Адаптивная длительность
Схемы с диапазоном времени (45-75 мин), подбираются под пользователя

### 4️⃣ Умный подбор
ИИ анализирует: возраст, пол, опыт, цели, время, здоровье

### 5️⃣ Интенсивность
🟢 Лёгкая | 🟡 Средняя | 🔴 Высокая

---

## Тестирование

### Новичок, 3 дня, 45 мин, похудеть
→ Full Body Classic (рекомендовано)

### Девушка, 4 дня, 60 мин, ягодицы
→ Upper/Lower + Glutes (рекомендовано)

### Опытный, 6 дней, 75 мин, масса
→ PPL 6x Classic (рекомендовано)

---

## Файлы

**Читать сначала:**
- 📄 `UPGRADE_SCHEMES.md` — инструкция обновления

**Полная документация:**
- 📄 `SCHEME_SELECTION_PRO.md` — все схемы, API, примеры

**Изменения:**
- 📄 `CHANGES_SUMMARY.md` — что изменилось

---

## Быстрые команды

### Проверить схемы
```sql
SELECT COUNT(*) FROM workout_schemes;
-- Должно быть: 26
```

### Посмотреть схему
```sql
SELECT name, days_per_week, min_minutes, max_minutes, intensity 
FROM workout_schemes 
WHERE id = 'full_body_3x_classic';
```

### Все схемы по дням
```sql
SELECT days_per_week, COUNT(*) 
FROM workout_schemes 
GROUP BY days_per_week 
ORDER BY days_per_week;
```

---

## Проблемы?

**Схемы не подбираются:**
```bash
# Проверьте OPENAI_API_KEY
echo $OPENAI_API_KEY

# Проверьте онбординг
psql -d database -c "SELECT COUNT(*) FROM onboardings;"
```

**Выбор времени не отображается:**
```bash
# Обновите код
cd webapp
git pull origin main  # или ваш branch
npm install
```

---

**Вопросы?** → `UPGRADE_SCHEMES.md`  
**Детали?** → `SCHEME_SELECTION_PRO.md`

---

✅ **Готово к продакшену**
