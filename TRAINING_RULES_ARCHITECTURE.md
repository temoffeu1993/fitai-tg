# Архитектура правил тренировок (непротиворечивая система)

> Как структура и объём работают вместе, а не противоречат друг другу

---

## 🎯 Два типа правил

### 1. СТРУКТУРНЫЕ правила (КАК тренироваться)
```typescript
structure: {
  compound: {
    count: [2, 3],      // Количество базовых упражнений
    sets: 4,            // Подходов на упражнение
    reps: "6-8",        // Повторений
    rest: 120,          // Отдых между подходами
    priority: 1         // Порядок выполнения (первыми!)
  },
  
  secondary: {
    count: [2, 3],
    sets: 3,
    reps: "8-12",
    rest: 90,
    priority: 2
  },
  
  isolation: {
    count: [2, 3],
    sets: 3,
    reps: "12-15",
    rest: 60,
    priority: 3
  }
}
```

**Определяет:**
- ✅ Сколько упражнений каждого типа
- ✅ Подходы/повторения/отдых
- ✅ Порядок выполнения
- ✅ Интенсивность

---

### 2. ОБЪЁМНЫЕ правила (СКОЛЬКО на каждую группу)
```typescript
targetMuscleVolume: {
  chest: {
    min: 16,
    max: 18,
    priority: "primary"
  },
  shoulders: {
    min: 14,
    max: 16,
    priority: "primary"
  },
  triceps: {
    min: 12,
    max: 14,
    priority: "secondary"  // работает синергистом
  }
}
```

**Определяет:**
- ✅ Минимум подходов на группу
- ✅ Максимум подходов на группу
- ✅ Приоритет группы

---

## ⚙️ Как они работают ВМЕСТЕ?

### Шаг 1: AI считает возможный объём от структуры

```typescript
// Push Day Advanced, 90 минут
structure: {
  compound: [2, 3] × 4 sets = 8-12 подходов
  secondary: [2, 3] × 3 sets = 6-9 подходов
  isolation: [2, 3] × 3 sets = 6-9 подходов
}

Итого от структуры: 20-30 подходов (прямых)
```

**НО!** Одно упражнение работает на НЕСКОЛЬКО групп:

```
Жим лёжа (compound):
- Грудь: 100% нагрузки → считаем 4 подхода
- Трицепс: ~50% нагрузки → считаем 2 подхода
- Передние дельты: ~30% → считаем 1.2 подхода

Армейский жим (compound):
- Плечи: 100% → 4 подхода
- Трицепс: ~40% → 1.6 подхода
```

С учётом синергистов:
```
20-30 прямых подходов
+ 10-15 подходов от синергистов
= 30-45 подходов ЭФФЕКТИВНЫХ
```

---

### Шаг 2: AI распределяет упражнения по группам

```typescript
// ЦЕЛЬ (от targetMuscleVolume):
chest: 16-18 подходов
shoulders: 14-16 подходов
triceps: 12-14 подходов

// ПЛАН AI:
COMPOUND (приоритет 1):
1. Жим лёжа: 4×6-8
   → Грудь: +4, Трицепс: +2, Плечи: +1
   
2. Жим на наклонной: 4×6-8
   → Грудь: +4, Трицепс: +2, Плечи: +1
   
3. Армейский жим: 4×6-8
   → Плечи: +4, Трицепс: +1.5

Итого compound: 3 упражнения ✅
Грудь: 8, Плечи: 6, Трицепс: 5.5

SECONDARY (приоритет 2):
4. Отжимания на брусьях: 3×8-12
   → Грудь: +3, Трицепс: +3
   
5. Жим гантелей сидя: 3×8-12
   → Плечи: +3

Итого secondary: 2 упражнения ✅
Грудь: 11, Плечи: 9, Трицепс: 8.5

ISOLATION (приоритет 3):
6. Махи в стороны: 3×12-15
   → Средние дельты: +3
   
7. Разводки гантелей: 3×12-15
   → Грудь: +3
   
8. Французский жим: 3×12-15
   → Трицепс: +3

Итого isolation: 3 упражнения ✅
Грудь: 14, Плечи: 12, Трицепс: 11.5

ПРОВЕРКА:
- Грудь: 14 < 16 ❌ Нужно ещё!
- Плечи: 12 < 14 ❌ Нужно ещё!
- Трицепс: 11.5 < 12 ⚠️ Почти достаточно

AI ДОБАВЛЯЕТ:
9. Махи на заднюю дельту: 3×12-15
   → Плечи: +3 → Итого: 15 ✅
   
10. Кроссоверы: 3×12-15
    → Грудь: +3 → Итого: 17 ✅

ИТОГО: 10 упражнений (было max 9 от структуры)
```

---

## 🔧 Правило приоритета

### Если структура и объём не сходятся:

```
ПРИОРИТЕТ 1: Структура (нельзя нарушать!)
- Минимум 2 compound
- Порядок: compound → secondary → isolation

ПРИОРИТЕТ 2: Объём по группам (гибкий!)
- Минимумы - это цель, а не жёсткое правило
- Можно добавить +1-2 упражнения сверх структуры
- Можно снизить минимумы на 10-20% если не хватает времени
```

---

## 📏 Формулы согласования

### 1. Максимальное количество упражнений

```typescript
maxExercises = 
  structure.compound.count.max +
  structure.secondary.count.max +
  structure.isolation.count.max

// Для Push Day: 3 + 3 + 3 = 9 упражнений
```

### 2. Максимальный возможный объём от структуры

```typescript
maxVolumeFromStructure = 
  (compound.count.max × compound.sets) +
  (secondary.count.max × secondary.sets) +
  (isolation.count.max × isolation.sets)

// Для Push Day: (3×4) + (3×3) + (3×3) = 12 + 9 + 9 = 30 подходов
```

### 3. Требуемый объём по группам

```typescript
requiredVolume = 
  chest.min + shoulders.min + triceps.min

// Для Push Day Advanced 90мин: 16 + 14 + 12 = 42 подхода
```

### 4. Проверка согласованности

```typescript
if (requiredVolume > maxVolumeFromStructure × 1.5) {
  console.warn("⚠️ Объём по группам СЛИШКОМ высокий для структуры!");
  // Решение: увеличить maxExercises ИЛИ снизить минимумы
}

if (requiredVolume < maxVolumeFromStructure × 0.5) {
  console.warn("⚠️ Объём по группам СЛИШКОМ низкий для структуры!");
  // Решение: увеличить минимумы ИЛИ уменьшить maxExercises
}
```

**Для Push Day:**
```
requiredVolume = 42 подхода
maxVolumeFromStructure = 30 подходов

42 / 30 = 1.4 (140% от структуры)
```

**Это ОК!** Потому что:
- Синергисты дают +10-15 подходов
- Можно добавить +1-2 упражнения (31-37 подходов)
- С синергистами: 37 × 1.4 = ~52 эффективных подхода ✅

---

## ✅ Правильная конфигурация Push Day Advanced 90 мин

```typescript
export const PPL_PUSH_DAY_RULES: DayTrainingRules = {
  name: "Push Day",
  focus: "Грудь, плечи, трицепс — все толкающие движения",
  
  // СТРУКТУРА (КАК тренироваться)
  structure: {
    totalExercisesRange: [6, 10],  // гибкий диапазон
    
    compound: {
      count: [2, 3],
      sets: (level) => level === "advanced" ? 5 : 4,
      reps: "6-8",
      rest: 120,
      priority: 1,
      notes: "Тяжёлые базовые жимы - фундамент"
    },
    
    secondary: {
      count: [2, 3],
      sets: 3,
      reps: "8-12",
      rest: 90,
      priority: 2,
      notes: "Проработка под другими углами"
    },
    
    isolation: {
      count: [2, 4],  // гибко! от 2 до 4
      sets: 3,
      reps: "12-15",
      rest: 60,
      priority: 3,
      notes: "Изоляция мелких мышц"
    }
  },
  
  // ОБЪЁМ ПО ГРУППАМ (СКОЛЬКО на каждую)
  targetMuscleVolume: {
    chest: {
      advanced: {
        90: { min: 16, max: 18, priority: "primary" }
      }
    },
    shoulders: {
      advanced: {
        90: { min: 14, max: 16, priority: "primary" }
      }
    },
    triceps: {
      advanced: {
        90: { min: 10, max: 14, priority: "secondary" }
      }
    }
  },
  
  // ПРАВИЛА СОГЛАСОВАНИЯ
  reconciliationRules: {
    // Если не хватает объёма:
    ifVolumeShort: "add_isolation",  // добавить изоляцию
    
    // Если слишком много объёма:
    ifVolumeExcess: "reduce_isolation",  // убрать изоляцию
    
    // Можно ли выходить за maxExercises?
    allowExtraExercises: true,  // да, если нужно для минимумов
    maxExtraExercises: 2,       // но не больше +2
    
    // Учитывать синергистов?
    countSynergists: true,
    synergistMultiplier: {
      compound: 0.5,   // базовые дают 50% на синергистов
      secondary: 0.3,  // вторичные дают 30%
      isolation: 0     // изоляция не даёт
    }
  }
}
```

---

## 🎯 Алгоритм AI

```typescript
async function buildWorkout(rules: DayTrainingRules, profile: UserProfile) {
  // 1. Определяем параметры
  const level = profile.experience;  // "advanced"
  const time = profile.timeAvailable;  // 90
  
  // 2. Берём целевой объём
  const targets = rules.targetMuscleVolume;
  const chestMin = targets.chest[level][time].min;  // 16
  const shouldersMin = targets.shoulders[level][time].min;  // 14
  const tricepsMin = targets.triceps[level][time].min;  // 10
  
  // 3. Выбираем упражнения по структуре
  let exercises = [];
  
  // Compound (2-3 упражнения)
  exercises.push(...selectCompound(2-3));
  
  // Secondary (2-3 упражнения)
  exercises.push(...selectSecondary(2-3));
  
  // Isolation (2-4 упражнения)
  exercises.push(...selectIsolation(2-4));
  
  // 4. Считаем фактический объём
  const actualVolume = calculateVolume(exercises, {
    countSynergists: true,
    synergistMultiplier: rules.reconciliationRules.synergistMultiplier
  });
  
  // 5. Проверяем минимумы
  if (actualVolume.chest < chestMin) {
    // Добавляем упражнение на грудь
    exercises.push(selectIsolation("chest"));
  }
  
  if (actualVolume.shoulders < shouldersMin) {
    exercises.push(selectIsolation("shoulders"));
  }
  
  if (actualVolume.triceps < tricepsMin) {
    exercises.push(selectIsolation("triceps"));
  }
  
  // 6. Проверяем максимумы
  if (exercises.length > rules.structure.totalExercisesRange[1]) {
    if (!rules.reconciliationRules.allowExtraExercises) {
      // Убираем лишние изоляции
      exercises = removeLowestPriority(exercises);
    }
  }
  
  return exercises;
}
```

---

## ✅ Итоговая гармония

```
СТРУКТУРА задаёт:
- Схему: 2-3 compound, 2-3 secondary, 2-4 isolation
- Параметры: подходы, повторения, отдых
- Порядок: compound первыми!

ОБЪЁМ задаёт:
- Минимумы: грудь 16+, плечи 14+, трицепс 10+
- Максимумы: не перегружаем
- Приоритеты: какие группы важнее

СОГЛАСОВАНИЕ:
- Если не хватает → добавляем изоляцию
- Если много → убираем изоляцию
- Структура имеет приоритет (порядок нельзя нарушать!)
- Объём гибкий (можно ±10-20%)

РЕЗУЛЬТАТ:
- Правильная структура ✅
- Достаточный объём ✅
- Научный подход ✅
- Нет противоречий ✅
```

---

## 🎯 Пример финальной тренировки

```
Push Day Advanced 90 мин:

COMPOUND (приоритет 1):
1. Жим лёжа: 5×6-8, 120с → Грудь +5, Трицепс +2.5, Плечи +1
2. Жим на наклонной: 5×6-8, 120с → Грудь +5, Трицепс +2.5, Плечи +1
3. Армейский жим: 5×6-8, 120с → Плечи +5, Трицепс +2

SECONDARY (приоритет 2):
4. Отжимания на брусьях: 3×8-12, 90с → Грудь +3, Трицепс +3
5. Жим гантелей сидя: 3×8-12, 90с → Плечи +3

ISOLATION (приоритет 3):
6. Махи в стороны: 3×12-15, 60с → Средние дельты +3
7. Разводки: 3×12-15, 60с → Грудь +3
8. Французский жим: 3×12-15, 60с → Трицепс +3

ПРОВЕРКА:
Упражнений: 8 (в диапазоне 6-10) ✅
Грудь: 5+5+3+3 = 16 (min 16) ✅
Плечи: 1+1+5+3+3 = 13 (min 14) ⚠️ → добавим упражнение
Трицепс: 2.5+2.5+2+3+3 = 13 (min 10) ✅

ДОБАВЛЯЕМ:
9. Махи на заднюю дельту: 3×12-15, 60с → Плечи +3

ИТОГО:
9 упражнений, 39 подходов
Грудь: 16 ✅
Плечи: 16 ✅
Трицепс: 13 ✅

Структура соблюдена ✅
Объём достигнут ✅
Нет противоречий ✅
```

---

**Это и есть непротиворечивая система!** 🎯

